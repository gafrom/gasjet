%h1 Производители

%table
  %thead
    %tr
      %th= t '.image'
      %th= t '.name'
      %th= t '.slug'
      %th= t '.web_site'
      %th= t '.description'
      %th
      %th

  %tbody
    - @resources.each do |producer|
      %tr
        %td
          = link_to [:admin, producer] do
            - if producer.image
              = image_tag producer.image.url, class: 'image__small'
            - else
              = 'Изображения нет'
        %td= producer.name
        %td= producer.slug
        %td= producer.web_site
        %td= producer.description
        %td= link_to 'Edit', edit_admin_producer_path(producer)
        %td= link_to 'Destroy', producer, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Producer', new_admin_producer_path
